legendhacker(in=urlprofile("httsp:t.me/ll_ll_LegendHacker_IN_ll_ll").url{buttons.pin}))
  buttons.pin(os).reboot({var}).ON({|buttons|}).in
          pin(:opnner(:os)>OFF<start_on)).legend return
           class close
